
---
- name: play
  hosts: node1.example.com
  tasks:  
  - name: facts
    debug:
     msg: |
           my vm details "{{ ansible_distribution }}" "{{ ansible_distribution_file_path }}" "{{ ansible_distribution_version }}"
           my vm ipaddress "{{ ansible_facts['default_ipv4']['address'] }}"
